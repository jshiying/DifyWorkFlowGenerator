# 用户需求样例集

> 以下是可直接输入到 Dify工作流生成器 中的自然语言需求样例，按复杂度从低到高排列。  
> 将任意一条复制粘贴到聊天框，即可生成对应的 Dify 工作流 YAML 文件。

---

## 🟢 入门级（单链路，1-3个节点）

---

### 样例1：商品文案生成器

```
帮我做一个电商商品描述生成工具。

用户输入：
- 商品名称（短文本）
- 商品核心卖点（段落，最多500字）
- 目标受众（例：年轻女性、商务人士）

输出：一段300字以内、吸引人的商品详情页文案，语气活泼，带有表情符号。
```

---

### 样例2：会议纪要整理

```
做一个会议纪要整理工具。

用户粘贴一段会议录音的文字转录稿（段落输入），
AI自动整理成标准会议纪要格式，包含：
1. 会议主题
2. 参会人员（如提到）
3. 讨论要点（列表）
4. 决议事项
5. 下一步行动计划（含负责人和截止日期，如提到）

直接输出整理后的纪要文本。
```

---

### 样例3：合同关键信息提取

```
做一个合同信息提取工具。

用户上传一份合同PDF文件，系统自动提取以下字段：
- 甲方名称
- 乙方名称
- 合同金额
- 合同期限（开始日期/结束日期）
- 付款方式
- 违约条款摘要

以结构化格式输出上述信息。
```

---

### 样例4：代码注释生成器

```
做一个代码注释自动生成工具。

用户输入一段代码（段落输入，支持Python/JavaScript/Java），
AI分析代码逻辑，为每个函数/方法自动添加注释，
输出带注释的完整代码。

要求注释风格为中文，简洁清晰，不超过每行代码2倍的注释量。
```

---

## 🟡 中级（多路分支或外部调用，4-7个节点）

---

### 样例5：简历智能评分系统

```
帮我做一个简历评分工具。

用户上传一份PDF格式的简历，以及目标岗位名称（短文本）。

系统流程：
1. 提取简历全文
2. 用LLM分析简历与目标岗位的匹配程度
3. 用参数提取器提取以下结构化结果：
   - 总分（0-100的数值）
   - 优势亮点（3-5条，字符串数组）
   - 改进建议（3-5条，字符串数组）
   - 是否推荐进入下一轮（布尔值，用0/1表示）
4. 用模板生成格式美观的评估报告

最终输出评估报告文本。
```

---

### 样例6：客服智能路由系统

```
做一个电商客服自动路由和回答系统。

用户输入问题内容，系统自动判断属于哪类问题并分别处理：

- 产品咨询：从产品知识库（数据集ID：your-product-dataset-id）检索后回答
- 订单查询：调用订单API（GET https://api.example.com/orders，传入用户输入的订单号参数），解析响应后回答
- 投诉/退款：由LLM生成安抚话术和处理建议

三类问题统一用answer节点输出回答内容。

输入字段：
- 用户问题（短文本）
- 订单号（短文本，选填）
```

---

### 样例7：YouTube学习笔记生成器

```
做一个YouTube视频学习笔记生成工具。

用户输入YouTube视频链接，系统自动：
1. 从URL中解析出视频ID（用代码节点实现正则提取）
2. 调用YouTube字幕API获取英文字幕
3. 用LLM生成学习笔记，包含：
   - 视频核心主题（1-2句话）
   - 重点知识点（5-8条）
   - 关键术语解释
4. 同时用参数提取器提取关键词列表（5个词）和难度等级（入门/中级/高级）
5. 用模板将笔记格式化输出

最终输出：格式化的学习笔记 + 关键词列表 + 难度等级。
```

---

### 样例8：竞品官网自动分析

```
帮我做一个竞品分析助手。

用户输入竞品官网URL，系统自动抓取页面内容，然后并行执行两个任务：
- 任务A：用参数提取器提取产品名称、定价信息、核心功能列表
- 任务B：用LLM分析该产品的优势和劣势

两个任务完成后，将结果合并，再用一个LLM生成完整的竞品分析报告。

输出：竞品分析报告（包含基本信息、优劣势分析、应对建议）。
```

---

## 🔴 高级（复合路径 + 多种节点组合，7个节点以上）

---

### 样例9：合同风险审查系统

```
帮我做一个合同风险审查系统，用户以对话框形式使用：

用户上传合同文件（PDF/Word），系统执行以下流程：

1. 提取合同全文
2. 判断合同类型（采购合同 / 服务合同 / 保密协议，共三类）
3. 根据合同类型分别检索对应的知识库：
   - 采购合同 → 数据集ID：procurement-rules-dataset
   - 服务合同 → 数据集ID：service-rules-dataset
   - 保密协议 → 数据集ID：nda-rules-dataset
4. LLM对照检索到的规范，分析合同风险
5. 用参数提取器提取风险等级（高风险 / 低风险）
6. 根据风险等级条件分支：
   - 高风险：自动POST请求发送邮件通知（https://api.example.com/notify），同时生成详细风险报告
   - 低风险：只生成简单的风险摘要
7. 最终将报告内容和风险等级一起输出
```

---

### 样例10：多语言内容审核与发布系统

```
做一个社交媒体内容审核和多语言发布系统。

用户输入：
- 待发布的内容文本（段落）
- 目标平台（选择：微博 / Twitter / LinkedIn）
- 目标语言（选择：中文 / 英文 / 双语）

系统流程：
1. 先用LLM对内容进行合规审核，检查是否存在违规风险
2. 用参数提取器提取审核结果：通过/不通过、风险类型（如有）、风险等级（高/中/低）
3. IF/ELSE判断：
   - 审核不通过（高风险）：直接输出拒绝原因，流程结束
   - 审核通过或低风险：继续下一步
4. 根据目标平台和语言，用LLM将内容改写成适合该平台的风格：
   - 微博：活泼、带话题标签、140字内
   - Twitter：英文、简洁有力、280字符内
   - LinkedIn：专业正式、可较长
5. 如果目标语言为"双语"，同时生成中英文两个版本（两个并行LLM节点）
6. 用模板格式化最终发布文案
7. 调用发布API（POST https://api.social.example.com/publish）提交内容
8. 输出：最终文案 + 发布状态
```

---

### 样例11：数据API智能分析管道

```
做一个数据API智能分析工具。

用户输入：
- 数据API地址（短文本，GET接口，直接返回JSON数组）
- 分析目标描述（段落，例如：找出销售额异常下降的时段）

系统流程：
1. 调用用户提供的API获取数据
2. 用JSON Parse节点提取data字段
3. 用代码节点判断数据量：
   - 数据超过100条：随机采样50条
   - 数据不超过100条：全量使用
4. 将处理后的数据送入LLM，根据用户的分析目标进行智能分析
5. 用参数提取器提取结构化结论：
   - 主要发现（字符串数组，3-5条）
   - 异常点描述（字符串，如无则为"无异常"）
   - 建议行动（字符串数组，2-3条）
   - 数据质量评分（数值，0-10）
6. 用模板生成完整分析报告
7. 输出分析报告和各结构化字段
```

---

### 样例12：智能简历筛选与面试题生成

```
做一个HR招聘辅助系统，包含简历筛选和面试题生成两个功能。

用户输入：
- 岗位名称（短文本）
- 岗位要求描述（段落）
- 候选人简历（文件上传，PDF格式）

系统流程：
1. 从简历文件提取文本
2. 同时并行执行：
   - LLM评估简历与岗位的匹配度，生成评估意见
   - 参数提取器从简历中提取候选人姓名、工作年限、技能标签列表
3. 将两路结果合并后，再由参数提取器综合提取：
   - 匹配分数（0-100的数值）
   - 是否推荐（0=不推荐，1=推荐）
   - 候选人主要优势（字符串数组）
4. 用IF/ELSE判断匹配分数：
   - 分数≥70（推荐）：
     - 用LLM根据简历和岗位要求生成5道定制化面试题
     - 用模板生成"推荐通过"报告（含面试题）
   - 分数<70（不推荐）：
     - 用模板生成"暂不推荐"报告（含差距分析）
5. 输出：筛选报告（含匹配分数、推荐结论）+ 面试题（如有）
```

---

## 📌 使用说明

1. **复制任意一条样例**，粘贴到 Dify工作流生成器 的对话框中
2. AI 会根据需求描述生成对应的 **Dify 工作流 YAML 文件**
3. 将生成的 YAML 内容保存为 `.yml` 文件，在 Dify 平台的工作流界面选择 **「导入DSL」** 即可使用
4. 如果生成的工作流不符合预期，可以继续在对话中**追加描述**进行修正，例如：
   - "请把输出改为answer节点而不是end节点"
   - "知识库数据集ID改为 abc-123"
   - "在LLM之前增加一个内容长度判断，超过2000字就先做摘要"

---

## 💡 需求描述技巧

| 技巧 | 说明 | 示例 |
|------|------|------|
| **明确输入类型** | 说明输入是文件/短文本/段落/选择 | "用户上传PDF文件" / "输入一段文字" |
| **描述分支条件** | 明确说明分叉的判断依据 | "高风险走A路径，低风险走B路径" |
| **指定外部接口** | 提供API的URL和请求方式 | "POST请求发送到 https://xxx.com/api" |
| **列出知识库** | 说明要用哪个数据集 | "从产品手册知识库（ID: xxx）检索" |
| **描述输出格式** | 说明最终要输出什么内容 | "输出风险等级和详细报告两个字段" |
| **说明并行需求** | 明确哪些任务可以同时执行 | "A和B两个分析任务同时进行" |
